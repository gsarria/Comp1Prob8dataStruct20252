#include <stdio.h>
#include <stdlib.h>
int main(void)
{
    int r,c; 
    scanf("%d %d",&r,&c);
    // TODO: leer matriz contigua r*c, promedio (double), imprimir 0 si < promedio, si no mismo valor
    // TODO: free
    return 0;
}
